describe("Calculator tests", () => {
    test('adding 1 + 2 should return 3', () => {
        expect(1+2).toBe(3);
    });
    test('adding 5 + 5 should return 10', () => {
        expect(5+5).toBe(10);
    });
})